{% extends 'base.html.twig' %}

{% block body %}
    <h1>Ligneentree</h1>

    <table>
        <tbody>
            <tr>
                <th>Qteentree</th>
                <td>{{ ligneentree.qteentree }}</td>
            </tr>
            <tr>
                <th>Numlingeentree</th>
                <td>{{ ligneentree.numlingeentree }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('ligneentree_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('ligneentree_edit', { 'numlingeentree': ligneentree.numlingeentree }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
