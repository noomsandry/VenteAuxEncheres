{% extends 'base.html.twig' %}

{% block body %}
    <h1>Categorieper</h1>

    <table>
        <tbody>
            <tr>
                <th>Nomcategpers</th>
                <td>{{ categoriePer.nomcategpers }}</td>
            </tr>
            <tr>
                <th>Numcategpers</th>
                <td>{{ categoriePer.numcategpers }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('categoriepers_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('categoriepers_edit', { 'numcategpers': categoriePer.numcategpers }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
