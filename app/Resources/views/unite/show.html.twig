{% extends 'base.html.twig' %}

{% block body %}
    <h1>Unite</h1>

    <table>
        <tbody>
            <tr>
                <th>Libelleunit</th>
                <td>{{ unite.libelleunit }}</td>
            </tr>
            <tr>
                <th>Numunit</th>
                <td>{{ unite.numunit }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('unite_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('unite_edit', { 'numunit': unite.numunit }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
